apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: commerce-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: ghcr.io/arda/distributed-commerce-core:inventory-service
          ports:
            - containerPort: 8083
          env:
            - name: APP_NAME
              value: inventory-service
            - name: HTTP_ADDR
              value: ":8083"
            - name: POSTGRES_DSN
              value: postgres://app:app@postgres:5432/commerce?sslmode=disable
            - name: NATS_URL
              value: nats://nats:4222
            - name: REDIS_ADDR
              value: redis:6379
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8083
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8083
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: commerce-core
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8083
      targetPort: 8083
