apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: commerce-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: ghcr.io/arda/distributed-commerce-core:order-service
          ports:
            - containerPort: 8081
          env:
            - name: APP_NAME
              value: order-service
            - name: HTTP_ADDR
              value: ":8081"
            - name: POSTGRES_DSN
              value: postgres://app:app@postgres:5432/commerce?sslmode=disable
            - name: NATS_URL
              value: nats://nats:4222
            - name: REDIS_ADDR
              value: redis:6379
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: commerce-core
spec:
  selector:
    app: order-service
  ports:
    - port: 8081
      targetPort: 8081
